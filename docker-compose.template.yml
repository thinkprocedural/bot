# /docker-compose.yml

version: "3.8"

services:
    bot:
        build:
            context: .
            dockerfile: Dockerfile

        container_name: bot
        restart: unless-stopped
        environment:
            TOKEN: "DISCORD_TOKEN"
            PREFIX: "!"
