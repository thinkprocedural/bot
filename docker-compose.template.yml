# /docker-compose.yml

version: "3.8.5"

services:
    bot:
        build:
            context: .
            dockerfile: dockerfile

        container_name: bot
        restart: unless-stopped
        environment:
            TOKEN: "DISCORD_TOKEN"
            PREFIX: "!"
